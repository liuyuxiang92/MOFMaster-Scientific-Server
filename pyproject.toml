[project]
name = "mof-tools"
version = "1.0.0"
description = "Simplified MOF research tools via MCP"
requires-python = ">=3.10"
dependencies = [
    "mcp[server]",
    "ase",
]

[project.scripts]
mof-tools = "main:mcp.run"
